<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.features.common.dao.UserGroupDao">

    <sql id="userGroupTable">
        id, group_name, parent_id, ctime, mtime
    </sql>

    <insert id="addUserGroup">
      INSERT INTO sys.user_group
        (id, group_name, parent_id, ctime)
      VALUES
        (#{id}, #{groupName}, #{parentId}, now())
    </insert>

    <delete id="removeUserGroup">
      DELETE FROM
          sys.user_group
      WHERE id = #{id}
    </delete>

    <select id="listUserGroup" resultType="userGroupPo">
        SELECT
          <include refid="userGroupTable"/>, user_id
        FROM sys.user_group
        LEFT JOIN sys.user_group_associate uga ON uga.group_id = ug.id
        <where>
            <if test="userId != null and userId != ''">
                uga.user_id = #{userId}
            </if>
        </where>
    </select>

    <select id="getUserGroupByUserId" resultType="userGroup">
        SELECT
          <include refid="userGroupTable"/>
        FROM
          sys.user_group ug
        JOIN sys.user_group_associate uga ON uga.group_id = ug.id
        WHERE uga.user_id = #{userId}
    </select>

</mapper>