<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.features.security.dao.UserDao">

    <sql id="userTable">
        u.id, u.user_name, u.delete, u.ctime, u.mtime
    </sql>

    <insert id="addUser">
        INSERT INTO
          sys.user
          (id, user_name, passwor, ctime)
        VALUES
          (#{id}, #{userName}, #{password}, now())
    </insert>

    <delete id="removeUserById">
        UPDATE sys.user SET
            delete = TRUE
        WHERE id = #{id}
    </delete>

    <select id="listUserByGroup" resultType="user">
        SELECT
          <include refid="userTable"/>
        FROM
          sys.user AS u
        JOIN
          sys.user_group_associate AS ug ON u.id = ug.user_id
        WHERE
        group_id = #{groupId}
        AND u.delete = FALSE
    </select>

    <select id="getUserByName" resultType="user">
        SELECT
          <include refid="userTable"/>
        FROM
          sys.user u
        JOIN sys.user_group_associate uga
        ON u.id = uga.user_id
        WHERE u.user_name = #{name}
        AND u.delete = FALSE
    </select>


</mapper>